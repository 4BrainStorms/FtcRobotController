package org.firstinspires.ftc.teamcode.config;

import com.acmerobotics.dashboard.config.Config;

/**
 * Dashboard-configurable toggle for selecting launch zone.
 * If true, use front launch zone; if false, use back launch zone.
 */
@Config
public class LaunchZoneToggle {
    public static boolean useFrontZone = true;
}